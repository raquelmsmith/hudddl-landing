import React, { Component } from "react";
import logo from "../hudddl-logo.png";

const Logo = props => (
  <div className="logo">
    <img src={logo} className="hudddl-logo" alt="huddl logo" />
  </div>
);

export default Logo;
